page.js

------------------------------------------


import { Room } from "./room";

export interface Page<T> {
    page: number;
    size: number;
    totalElements?: number;
    totalPage: number;
    content: T[];
}


------------------------------------------

          <!-- Pagination -->
          <div class="d-flex justify-content-between align-items-center">
            <div>
              Page <strong>{{ p.page + 1 }}</strong> of <strong>{{ p.totalPage }}</strong>
              &nbsp;|&nbsp; Total: <strong>{{ p.totalElements }}</strong>
            </div>
            <div class="btn-group">
              <button class="btn btn-outline-secondary btn-sm" (click)="first()" [disabled]="p.page === 0">« First</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="prev()"  [disabled]="p.page === 0">‹ Prev</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="next()"  [disabled]="p.page >= p.totalPage-1">Next ›</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="last()"  [disabled]="p.page >= p.totalPage -1">Last »</button>
            </div>
          </div>
        } @else {
          <div class="alert alert-info">No rooms found.</div>
        }

============================================================

Explain:

            <div class="btn-group">
              <button class="btn btn-outline-secondary btn-sm" (click)="first()" [disabled]="p.page === 0">« First</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="prev()"  [disabled]="p.page === 0">‹ Prev</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="next()"  [disabled]="p.page >= p.totalPage-1">Next ›</button>
              <button class="btn btn-outline-secondary btn-sm" (click)="last()"  [disabled]="p.page >= p.totalPage -1">Last »</button>
            </div>





















