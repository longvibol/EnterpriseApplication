We get the message now from the kafka, now we can consume the message and save in the database 

Step by Step 

1. Configer property 

- we no need to create the controller 

1- setup the consumer kafka 
setup port of kafa where is the kafka is located 


in the application.yml

kafka:
    bootstrap-servers: localhost:9092
    consumer:
      #group-id: visitor-room-projection
      group-id: visitor-room-projection-3
      auto-offset-reset: earliest # (start from beginning)
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      enable-auto-commit: false
      properties:
        spring.json.trusted.packages: "com.piseth.java.school.*"
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.piseth.java.school.roomservice.message.event.RoomEventEnvelope
      max-poll-records: 100        

